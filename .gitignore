# secrets — never commit (credentials live in the project dir, gitignored)
data/tableau_pat.json
data/airtable_token
.env

# generated / local data
data/*.db
data/*.db-wal
data/*.db-shm
data/*.csv
data/notes_local.json
__pycache__/
*.pyc

# docs/ is published to the gh-pages branch by deploy.py, never committed to main
docs/

# generated reports (kept in the project data store, not committed)
data/reports/

# logs
data/*.log
data/last_historical_fetch

# launchd once-per-day success marker (runtime state)
data/last_success_date
